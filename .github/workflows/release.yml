name: Release

on:
  workflow_call:
    inputs:
      prerelease:
        required: true
        type: boolean
        description: 'Whether this is a prerelease (beta) version'
      branch:
        required: true
        type: string
        description: 'Branch being released'
    outputs:
      version:
        description: 'The new version number'
        value: ${{ jobs.release.outputs.version }}

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.release.outputs.version }}
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      - name: Cache Bun dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run Nx Release
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Ensure tags are available locally for detection
          git fetch --tags --force
          
          # Stage the lock file if it exists to prevent git errors
          if [ -f "bun.lock" ]; then
            git add bun.lock
          elif [ -f "bun.lockb" ]; then
            git add bun.lockb
          fi
          
          # Check if any version tags exist
          if ! git tag -l "v*" | grep -q .; then
            echo "No version tags found, using --first-release"
            if [ "${{ inputs.prerelease }}" = "true" ]; then
              # Let conventional commits determine the bump, with preid for beta
              bunx nx release --preid=beta --first-release --yes
            else
              bunx nx release --first-release --yes
            fi
          else
            if [ "${{ inputs.prerelease }}" = "true" ]; then
              # Let conventional commits determine the bump, with preid for beta
              bunx nx release --preid=beta --yes
            else
              bunx nx release --yes
            fi
          fi

          # Get the version from package.json after release
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Push changes
        run: git push origin ${{ inputs.branch }} --follow-tags
